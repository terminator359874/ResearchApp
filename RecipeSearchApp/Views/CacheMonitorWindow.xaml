<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="RecipeSearchApp.Views.CacheMonitorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    Title="Монитор Кеша">

    <Grid Padding="20" x:Name="RootGrid" ColumnSpacing="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Spacing="10">
            <TextBlock Text="Статистика LRU Кеша" FontSize="20" FontWeight="Bold"/>
            <TextBlock Text="{Binding StatsText}" FontSize="16" TextWrapping="Wrap"/>
        </StackPanel>

        <lvc:PieChart Grid.Column="1" Series="{Binding HitMissSeries}" LegendPosition="Right" />
    </Grid>
</Window>
